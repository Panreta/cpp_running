# 1. Function Call
    int* result = intersect(nums1, 4, nums2, 3, &returnSize);

so the function should be defined as:
    int* intersect(int* nums1, int nums1Size, int* nums2, int nums2Size, int* returnSize) 


---

